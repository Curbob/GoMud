// Practice Booth - Master Lock (Hard)
// Outside-lock flow: rewards are granted when entering from Lockpick Village after picking the booth.

function onEnter(user, room) {
    return true; // show room description only
}
